<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<BorderPane prefHeight="510.0" prefWidth="857.0" styleClass="dark-bg" stylesheets="@sigma.css" xmlns="http://javafx.com/javafx/17.0.12" xmlns:fx="http://javafx.com/fxml/1">
   <center>
      <StackPane BorderPane.alignment="CENTER">
         <children>
            <!-- Centered auth card: same structure as login -->
            <VBox StackPane.alignment="CENTER" alignment="CENTER" spacing="0.0" style="-fx-padding: 0; -fx-max-width: 420px;" styleClass="auth-card centered">
               <children>
                  <!-- Header inside the card -->
                  <HBox alignment="CENTER" prefHeight="52.0" style="-fx-background-color: #8B2E21; -fx-background-radius: 15 15 0 0; -fx-padding: 8 12;">
                     <children>
                        <Label text="Registrarse" style="-fx-text-fill: #ffffff; -fx-font-weight: bold;">
                           <font>
                              <Font size="17.0" />
                           </font>
                        </Label>
                     </children>
                  </HBox>

                  <!-- Content area: use a VBox with padding to layout fields cleanly -->
                  <VBox alignment="CENTER" spacing="10.0" style="-fx-padding: 16; -fx-background-radius: 0 0 15 15; -fx-background-color: -fx-background;">
                     <children>
                        <VBox alignment="CENTER_LEFT" spacing="6.0" style="-fx-pref-width: 100%;">
                           <children>
                              <Label styleClass="field-label" text="Nombre completo" />
                              <TextField fx:id="txtNombre" prefHeight="30.0" promptText="Nombre completo" styleClass="input" />
                              <Label styleClass="field-label" text="Correo" />
                              <TextField fx:id="txtEmail" prefHeight="30.0" promptText="Correo" styleClass="input" />
                              <Label styleClass="field-label" text="Contraseña" />
                              <StackPane>
                                 <children>
                                    <PasswordField fx:id="txtPassword" prefHeight="30.0" promptText="Contraseña" styleClass="input" StackPane.alignment="CENTER_LEFT" />
                                    <TextField fx:id="txtPasswordVisible" prefHeight="30.0" promptText="Contraseña" styleClass="input" visible="false" StackPane.alignment="CENTER_LEFT" />
                                    <Button fx:id="togglePasswordBtn" text="👁" styleClass="icon-btn" style="-fx-background-color: transparent; -fx-cursor: hand; -fx-font-size: 18px; -fx-text-fill: #8B2E21; -fx-padding: 0 8 0 8;" StackPane.alignment="CENTER_RIGHT" />
                                 </children>
                              </StackPane>
                           </children>
                        </VBox>

                        <Label fx:id="feedback" text="" />

                        <HBox alignment="CENTER" spacing="12.0">
                           <children>
                              <Button onAction="#onRegistrar" styleClass="primary-btn" text="Registrar" minWidth="110" />
                              <Button onAction="#onCancelar" styleClass="primary-btn" text="Cancelar" minWidth="110" />
                           </children>
                        </HBox>
                     </children>
                  </VBox>
               </children>
            </VBox>
         </children>
      </StackPane>
   </center>
</BorderPane>