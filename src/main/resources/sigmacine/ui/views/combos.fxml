<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.geometry.*?>
<BorderPane xmlns:fx="http://javafx.com/fxml" fx:controller="sigmacine.ui.controller.CombosController" styleClass="page-root" stylesheets="@sigma.css">
    <top>
        <VBox>
            <fx:include source="barra.fxml"/>
            <VBox spacing="6" BorderPane.alignment="CENTER">
                <HBox spacing="12" alignment="CENTER_LEFT" styleClass="page-header">
                    <Label text="Nuestros Combos" styleClass="section-title" />
                </HBox>
                <Separator />
            </VBox>
        </VBox>
    </top>
    <center>
        <ScrollPane fitToWidth="true" styleClass="menu-scroll">
            <content>
                <GridPane fx:id="gridCombos" hgap="24" vgap="24" alignment="TOP_CENTER">
                    <columnConstraints>
                        <ColumnConstraints percentWidth="50" />
                        <ColumnConstraints percentWidth="50" />
                    </columnConstraints>
                    <padding>
                        <Insets top="12" right="12" bottom="12" left="12" />
                    </padding>
                </GridPane>
            </content>
        </ScrollPane>
    </center>
</BorderPane>
