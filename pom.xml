<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.sigma.cine</groupId>
  <artifactId>proyecto-fundamentos-software</artifactId>
  <version>1.0-SNAPSHOT</version>
  <packaging>jar</packaging>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <java.version>21</java.version>
    <javafx.version>22.0.2</javafx.version>
    <!--para que sirva en diferentes SO -->
    <javafx.platform>win</javafx.platform>
  </properties>

  <dependencies>
    <!-- H2 Database -->
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <version>2.3.232</version>
    </dependency>
    
    <dependency>
      <groupId>org.mindrot</groupId>
      <artifactId>jbcrypt</artifactId>
      <version>0.4</version>
    </dependency>

    <!-- JavaFX -->
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-controls</artifactId>
      <version>${javafx.version}</version>
      <classifier>${javafx.platform}</classifier>
    </dependency>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-fxml</artifactId>
      <version>${javafx.version}</version>
      <classifier>${javafx.platform}</classifier>
    </dependency>
    <!-- JavaFX Web (WebView) for embedding trailers -->
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-web</artifactId>
      <version>${javafx.version}</version>
      <classifier>${javafx.platform}</classifier>
    </dependency>
    <!-- JUnit Jupiter for tests -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-api</artifactId>
      <version>5.10.0</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-engine</artifactId>
      <version>5.10.0</version>
      <scope>test</scope>
    </dependency>
    
    <!-- ZXing para generación de códigos de barras -->
    <dependency>
      <groupId>com.google.zxing</groupId>
      <artifactId>core</artifactId>
      <version>3.5.3</version>
    </dependency>
    <dependency>
      <groupId>com.google.zxing</groupId>
      <artifactId>javase</artifactId>
      <version>3.5.3</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- Java 21 -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
          <release>21</release>
        </configuration>
      </plugin>

         <plugin>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-maven-plugin</artifactId>
            <version>0.0.8</version>
            <configuration>
                <mainClass>sigmacine.ui.App</mainClass>
                <options>
                    <option>--add-opens=java.base/java.lang=ALL-UNNAMED</option>
                    <option>--add-opens=java.base/sun.misc=ALL-UNNAMED</option>
                    <option>--enable-native-access=javafx.graphics,javafx.media</option>
                    <option>-XX:+IgnoreUnrecognizedVMOptions</option>
                </options>
            </configuration>
         </plugin>

      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.5.1</version>
        <executions>
          <execution>
            <id>default-cli</id>
            <goals>
              <goal>java</goal>
            </goals>
            <configuration>
              <mainClass>sigmacine.infraestructura.configDataBase.TestConexion</mainClass>
              <classpathScope>runtime</classpathScope>
              <vmArgs>
                <vmArg>--enable-native-access=javafx.graphics,javafx.web,javafx.media</vmArg>
              </vmArgs>
            </configuration>
          </execution>

          <execution>
            <id>run-ui</id>
            <goals>
              <goal>java</goal>
            </goals>
            <configuration>
              <mainClass>sigmacine.ui.App</mainClass>
              <classpathScope>runtime</classpathScope>
              <vmArgs>
                <vmArg>--enable-native-access=javafx.graphics,javafx.web,javafx.media</vmArg>
              </vmArgs>
            </configuration>
          </execution>
          <execution>
            <id>simulate-purchase</id>
            <goals>
              <goal>java</goal>
            </goals>
            <configuration>
              <mainClass>sigmacine.tools.PurchaseSimulator</mainClass>
              <classpathScope>runtime</classpathScope>
              <vmArgs>
                <vmArg>--enable-native-access=javafx.graphics,javafx.web,javafx.media</vmArg>
              </vmArgs>
            </configuration>
          </execution>
        </executions>
      </plugin>

             
  <!-- Use modern Surefire to run JUnit 5 tests -->
      <plugin>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.1.2</version>
        <configuration>
          <!-- Combina argLine de Jacoco con opciones de JavaFX -->
          <argLine>@{argLine} --enable-native-access=javafx.graphics,javafx.web,javafx.media</argLine>
        </configuration>
      </plugin>

    <!-- Para el % de coverage de las pruebas .-->
             <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.11</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
    </plugins>
  </build>
</project>
